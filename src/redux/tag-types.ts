export enum tagTypes {
  roleRequests = "role-requests",
  store = "store",
  branch = "branch",
  category = "category",
}

export const tagTypesList = [
  tagTypes.roleRequests,
  tagTypes.store,
  tagTypes.branch,
  tagTypes.category,
];
